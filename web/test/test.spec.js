import { mount } from '@vue/test-utils'

// import Vue from 'vue'

describe('Counter', () => {
    //
    console.log(mount);

    it('renders the correct markup', () => {
        expect(true).toBe(true)
    })

})
